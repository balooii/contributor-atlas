pages:
  stage: deploy
  image: alpine:latest
  script:
    - apk add --no-cache curl
    - mkdir -p public/data/gimp
    - cp *.html *.js public/
    - cp -r css fonts lib public/
    - cp -r data/gimp public/data/
    - curl -L -f -o public/data/gimp/contributions.csv
        "https://gitlab.gnome.org/-/project/39188/uploads/d396b1908ea74ceb677b2a398407b665/contributions.csv"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
